# template-fastapi-react backend

FastAPI backend package for the `template-fastapi-react` full-stack template.

Use the repository root README for full-stack setup, CI, Docker Compose, and
copy/rename instructions.
